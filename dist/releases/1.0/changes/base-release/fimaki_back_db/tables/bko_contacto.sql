-- liquibase formatted sql
-- changeset FIMAKI_BACK_DB:1755640726236 stripComments:false  logicalFilePath:base-release/fimaki_back_db/tables/bko_contacto.sql runAlways:false runOnChange:false replaceIfExists:true failOnError:true
-- sqlcl_snapshot src/database/fimaki_back_db/tables/bko_contacto.sql:null:a373af8cdc78a99b9255cade9868047f1bd40735:create

create table fimaki_back_db.bko_contacto (
    id_contacto      number
        generated by default as identity
    not null enable,
    tipo_contacto_rc varchar2(20 char) not null enable,
    activo_sn        varchar2(1 char) default 'S' not null enable,
    id_persona       number not null enable,
    creado_por       varchar2(100 char) default sys_context('USERENV', 'SESSION_USER') not null enable,
    creado           timestamp(6) with local time zone default current_timestamp not null enable,
    modificado_por   varchar2(100 char),
    modificado       timestamp(6) with local time zone,
    cargo_rc         varchar2(20 char),
    notas            varchar2(2000 char),
    correo           varchar2(100 char),
    telefono_1       varchar2(20 char),
    telefono_2       varchar2(20 char)
);

alter table fimaki_back_db.bko_contacto
    add
        check ( activo_sn in ( 'S', 'N' ) ) enable;

alter table fimaki_back_db.bko_contacto
    add constraint bko_contacto_ck1
        check ( activo_sn in ( 'S', 'N' ) ) enable;

alter table fimaki_back_db.bko_contacto
    add constraint bko_contacto_pk primary key ( id_contacto )
        using index enable;

alter table fimaki_back_db.bko_contacto add constraint bko_contacto_uk1 unique ( id_persona )
    using index enable;

